{
  "entities": {
    "ConnectionTest": {
      "title": "ConnectionTest",
      "description": "Entity used to validate Firebase Firestore connectivity on application boot",
      "type": "object",
      "properties": {
        "status": {
          "type": "string",
          "description": "Connection status message"
        },
        "measuredAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the connection was verified"
        }
      },
      "required": ["status", "measuredAt"]
    },
    "HeroTrailer": {
      "title": "HeroTrailer",
      "description": "Premium cinematic trailers with associated logos intended for the hero section of the landing page.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "malId": { "type": "number" },
        "anilistId": { "type": "number" },
        "trailerUrl": { "type": "string", "format": "uri" },
        "logoUrl": { "type": "string", "format": "uri" },
        "order": { "type": "number" },
        "active": { "type": "boolean" }
      },
      "required": ["title", "malId", "anilistId", "trailerUrl", "logoUrl", "order", "active"]
    },
    "LibraryData": {
      "title": "LibraryData",
      "description": "Account-scoped subscribed anime listings for a user",
      "type": "object",
      "properties": {
        "animes": {
          "type": "array",
          "description": "List of subscribed anime details"
        }
      },
      "required": ["animes"]
    },
    "UserData": {
      "title": "UserData",
      "description": "User specific watch history, watch progress, and saved watch later lists.",
      "type": "object",
      "properties": {
        "history": {
          "type": "array",
          "description": "Watch history and progress entries"
        },
        "watch_later": {
          "type": "array",
          "description": "Watch later episodes"
        }
      }
    }
  },
  "firestore": {
    "test": {
      "schema": "ConnectionTest",
      "description": "Collection used for system connection checks"
    },
    "heroTrailers": {
      "schema": "HeroTrailer",
      "description": "Contains metadata for hero section premium trailers."
    },
    "libraries": {
      "schema": "LibraryData",
      "description": "Collection of user-subscribed libraries scoped by user ID"
    },
    "userData": {
      "schema": "UserData",
      "description": "Collection of user's personal private states (history and watch later lists) scoped by user ID"
    }
  }
}
